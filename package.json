{
  "name": "0708-site",
  "version": "1.0.0",
  "scripts": {
    "start": "npm --prefix server start",
    "preview:vercel": "node scripts/preview-vercel.mjs",
    "check": "node --check script.js && node --check server/index.js && node --check api/latest-tracks.mjs && node --check api/contact.mjs",
    "gen-profile": "node scripts/generate-profile.js"
  },
  "devDependencies": {
    "sharp": "^0.32.1"
  }
}
